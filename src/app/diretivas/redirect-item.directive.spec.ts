import { RedirectItemDirective } from './redirect-item.directive';

describe('RedirectItemDirective', () => {
  it('should create an instance', () => {
    const directive = new RedirectItemDirective();
    expect(directive).toBeTruthy();
  });
});

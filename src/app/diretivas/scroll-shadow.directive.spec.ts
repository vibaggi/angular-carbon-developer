import { ScrollShadowDirective } from './scroll-shadow.directive';

describe('ScrollShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollShadowDirective();
    expect(directive).toBeTruthy();
  });
});
